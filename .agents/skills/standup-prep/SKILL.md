---
name: standup-prep
description: Daily session startup coordinator. Sweeps all repos for uncommitted work, surfaces open PRs, produces a prioritised morning brief, writes it to disk, and optionally syncs tasks to Asana. Run this before any coding work. Calls the preflight, session context, repo status, WIP sweep, PR sweep, and Asana sync sub-skills in order.
---

> **`{{AI_ROOT}}`** must be configured before using this skill.
> In VS Code: `"github.copilot.chat.templateVariables": { "AI_ROOT": "/path/to/your/ai-workflow" }`
> All file paths in this skill use `{{AI_ROOT}}` as a template variable.

You are preparing the developer's workday. Complete all steps in order. **Do not skip steps.**

---

## Step 0 — Pre-flight

Run `/standup-prep-preflight`.

This runs `/check-prior-work`, `/load-oss-standards`, and `/load-dependency-chain` in sequence.
Any pre-standup work found will be stored for merging into the brief in Step 4.

---

## Step 1 — Load session context

Run `/load-session-context`.

---

## Step 2 — Repo status + WIP sweep

Run `/repo-status` to produce the dirty state table.

Then run `/wip-sweep` using the dirty state table from `/repo-status`.

---

## Step 3 — Open PR sweep

Run `/open-pr-sweep`.

---

## Step 4 — Produce the morning brief

Print the following, filled in with real data from Steps 0–3. Include any **pre-standup work** found in Step 0.

---

### 🌅 Standup — <YYYY-MM-DD>

**Hard deadline:** <from dependency-chain.md>
**Time to deadline:** <N weeks/days>

---

#### Critical path today

<Max 3 items. One sentence each. Be specific — not "work on Phase H" but "Build DonutChartCard in giselle-mui — required by first-branch Phase 3 viewer dashboard.">

---

#### WIP commits created this standup

| Repo | Branch | Files | Pushed  |
| ---- | ------ | ----- | ------- |
| ...  | ...    | N     | ✅ / ❌ |

_(or "All repos clean — no WIP sweep needed.")_

---

#### Open PRs

| #   | Repo | Title | Action needed |
| --- | ---- | ----- | ------------- |
| ... | ...  | ...   | ...           |

_(or "No open PRs.")_

---

#### Suggested first task

<If open PRs exist: "Run /respond-pr-review for PR #N in <repo> — clear PR debt before new code.">
<If no open PRs: The single most important critical-path task. Specific file path and what to build.>

---

#### Recent sessions (last 5)

| Date | What happened |
| ---- | ------------- |
| ...  | ...           |

---

#### What to skip today

<Max 2 items — tempting work that is NOT on the critical path.>

---

_(If an argument was passed to this skill, address it specifically after the brief.)_

---

## Step 5 — Write morning brief to file

Write the morning brief from Step 4 to a numbered file inside a dated subfolder:

```
{{AI_ROOT}}\Agents\Morning Briefs\YYYY-MM-DD\NN.md
```

**Rule — always use the subfolder, never a flat file:**

1. Check `{{AI_ROOT}}\Agents\Morning Briefs\YYYY-MM-DD\` for existing files.
2. Write to the next available number: `01.md` if the folder is empty or doesn't exist, `02.md` if `01.md` exists, `03.md` if `02.md` exists, etc.
3. Create the subfolder if it doesn't exist.
4. **Never overwrite an existing file. Never append to an existing file.**

File format:

```md
# Standup — YYYY-MM-DD / Standup N

<full morning brief content from Step 4>

---

_Generated by /standup-prep · Model: <model name> · Session: <session ID>_
```

Determine the actual output path before writing, then confirm:
`✅ Morning brief written to {{AI_ROOT}}\Agents\Morning Briefs\YYYY-MM-DD\NN.md`

---

## Step 6 — Asana sync (opt-in)

Run `/asana-sync`.

---

## Step 7 — Summary

Print a final one-line-per-repo status using **real data from Steps 0–6**. The example below is illustrative only — all values (repo names, branch names, file counts, paths, task counts) must come from this session's actual results:

```
✅ alexrebula       — clean
⚠️  giselle-mui     — snapshot committed to docs/20260523-dashboard-plan + chore/20260523-ci-config, pushed, PR not opened
✅ first-branch     — clean
✅ [all others]     — clean

📄 Morning brief written: {{AI_ROOT}}\Agents\Morning Briefs\YYYY-MM-DD\NN.md
📋 Asana sync: <N> tasks created in <project name> / Morning Briefs
```

The path is the actual output location from Step 5 — not a hardcoded value. Replace `YYYY-MM-DD` and `NN` with today's actual date and brief number.

---

## ✅ /standup-prep complete

All steps have run.

**What to do next:**

- If open PRs exist → run `/respond-pr-review` for the highest-priority one.
- If the morning brief listed a specific first task → start on it now.
- To wrap the session later → run `/session-wrap`.

You are now in a normal coding session.
