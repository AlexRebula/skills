/** Vertical padding for the section's `SectionContainer`. */
export const STATS_CONTAINER_PY = { xs: 4, md: 6 };

/** Bottom margin `sx` for the section's `SectionTitle`. */
export const STATS_TITLE_SX = { mb: { xs: 3, md: 4 } };
