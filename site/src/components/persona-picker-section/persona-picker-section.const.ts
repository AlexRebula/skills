/** `sx` for `SectionTitle` — matches `LandingStatsSection`'s own title spacing. */
export const PERSONA_PICKER_TITLE_SX = { mb: { xs: 3, md: 4 } };

/** `sx` for the router (`/ask-alex`) note below the persona filter row. */
export const PERSONA_PICKER_ROUTER_NOTE_SX = { mt: { xs: 2, md: 3 } };
