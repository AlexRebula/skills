{
  "private": true,
  "type": "module",
  "scripts": {
    "postinstall": "bash scripts/sync-agents-skills.sh",
    "test": "vitest run"
  },
  "devDependencies": {
    "prettier": "^3.0.0",
    "vitest": "^2.0.0"
  }
}
