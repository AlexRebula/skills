/**
 * @type {import("prettier").Config}
 * Need to restart IDE when changing configuration.
 */
const config = {
  semi: true,
  tabWidth: 2,
  endOfLine: 'lf',
  printWidth: 100,
  singleQuote: true,
  trailingComma: 'es5',
  proseWrap: 'never',
};

export default config;
